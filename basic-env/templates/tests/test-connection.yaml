apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "basic-env.fullname" . }}-test-connection"
  labels:
    {{- include "basic-env.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "basic-env.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
